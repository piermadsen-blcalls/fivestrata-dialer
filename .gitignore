# secrets — never commit real credentials
.env
.env.*
!.env.example

node_modules/
dist/
*.log
.DS_Store

# local env template with project-specific (non-secret) values
env.template

# Skills live in the fivestrata workspace repo (junctioned into .claude/skills locally)
.claude/skills/
.claude/worktrees/

# Vercel CLI project link (root-level after the repo-root relink 8/17)
.vercel/
