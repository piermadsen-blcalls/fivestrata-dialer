# 2026-08-18 — Shareout: calling engine, DID management, dialer controls

Distilled from Sean's meeting notes (Granola:
https://notes.granola.ai/t/5909d6e6-a2fe-4016-8def-bf871c99c243-00demib2).
Team shareout of the build status. Mostly a report-out of work already recorded in this
repo; **genuinely new items are marked 🆕** and have been threaded into the affected docs
(`did-lifecycle.md`, `campaign-delivery.md`, `open-questions.md`).

## 1. Calling engine status (report-out — all previously recorded)

- Voice mechanics ready for human testing.
- Scripts: windows, flooring, bathrooms, home warranty in place; **solar still needs a
  script** (stated as "five scripts built" in the room; solar is the fifth slot, pending).
- Handling doctrine: DNC/"no" triggers fast exit, "maybe" gets one push, firm no ends call.
- Viability monitor runs throughout calls estimating P(convert) in real time; ~50% of test
  calls ended early via the monitor, **zero convertible calls killed** (stated in the room
  as "zero false negatives"; the batteries measure it as zero false *positives* — no
  buyer's call ended early — same claim, see the 8/15 street-mix results).
- TTS long-tail fallback live; **capped at 3 uses per call** (loop/cost guard); nightly
  distillation identifies TTS-triggered moments and pre-generates clips
  (`scripts/tts-distill.ts`).
- 28/28 simulated buyers converted on the windows script (8/17 bench).
- Recordings in DB today; long-term = Snowflake at turn grain, 5-year retention.
- Clip variants: a clip needed twice in one call plays a variant, never identical audio.

## 2. DID management

Report-out of `did-lifecycle.md` (singles-not-blocks buying, CUSUM auto-quarantine,
reputation screening built-not-enabled at ~$100/mo, 1,500-lifetime retirement, all
thresholds config). One new thread:

- 🆕 **Caller-ID display name is an open decision with a legal wrinkle.** Current CNAM
  registration is `FIVESTRATA`, and it displayed as **"Five Strata" on Verizon during the
  live demo** (carrier-dependent rendering). Problems/opportunities raised:
  - "Five Strata" is **not a legal entity** — the DBA is **"New Strata"**; legal-entity
    confirmation needed for STIR/SHAKEN-adjacent registration.
  - **Contractors.com proposed** as the display name — strong branding fit for home
    services verticals.
  - Constraint: whatever name displays **must match the partner list on the lead forms**
    before any change goes live (consent-scope alignment).
  - Threaded into `did-lifecycle.md` §2 (registration) and the open-questions DID bullet.

## 3. Web control panel & infrastructure (report-out + process notes)

- Vercel (chosen over Netlify), auto-deploy on push, credit-based usage; Next.js.
- Repo shared; **PR workflow in place, no direct pushes to prod** (stated in the room —
  note the repo has been operating on direct-to-main; treat as the go-forward posture).
- Tenant/program structure shown: **AutoWeb and FiveStrata as the two tenants**.
- Per-program knobs: max dials, daily dial budget, lead cost ceiling, time-to-call,
  recall gaps.
- Campaign scheduler: budget + run-window in → per-lead dial schedule out
  (`campaign-delivery.md`, migration 0010).
- Snowflake integration mostly built, waiting on access via Shelly/Cesar (T8);
  not blocking.
- LeadConduit integration pending a sync with Joseph (T3).

## 4. API security & integration verification 🆕

- 🆕 **Today's Lead Intake API has no authentication.** Joseph has a **new authed
  endpoint launching with a call center later this week** — AICC should target that, not
  the documented one. (→ T6 note.)
- 🆕 **Some endpoints in the Confluence/CV documentation are wrong** — verify actual URLs
  with Joseph before hookup. (→ T4 note.)
- 🆕 **DNC endpoint hook likely built but unverified** — double-check before go-live, and
  **confirm DNC data passes through to the data warehouse**. (→ T11 note.)

## 5. Dialing algorithm 🆕

Design still open: FIFO vs LIFO, round-robin vs bunched attempts, speed-to-lead priority.

- 🆕 **Prism precedent**: FiveStrata's previous dialing algorithm ("Prism") assigned
  algorithms **at the per-lead level** with a **master prioritization layer** on top.
  Ask outstanding: share Prism's parameters — even high-level tuning variables (lead
  quality, speed-to-lead, time-of-day) are useful now.
- Goal: **different dialing algorithms per campaign**, enabling A/B testing.
- **CV is the primary use case and power user; AutoWeb gets a generic variant.**
- Threaded into `campaign-delivery.md` §5 (L3) as an open design input.

## 6. AutoWeb use case (report-out of the 8/17 call)

Ammie Lin / Jina Yoon use case restated: calling back scrubbed leads to validate them
with OEMs. Regional rollout suggested first — **~10 DIDs per area code** regionally rather
than buying nationally (consistent with the 8/17 post-call addendum's regional
partitioning proposal).

## Next steps (owners as stated)

| # | Item | Notes |
|---|---|---|
| 1 | Confirm DNC data hook passes to the data warehouse | Hook likely exists; verify before go-live |
| 2 | Verify correct API endpoints with Joseph + test DNC endpoint | Several documented endpoints wrong; lead-intake auth missing today — target the new authed endpoint |
| 3 | Get Snowflake access from Shelly | Integration built and waiting (T8); not blocking |
| 4 | Share Prism dialing-algorithm parameters | Even high-level tuning variables useful now |
| 5 | Decide STIR/SHAKEN caller-ID name | Contractors.com preferred; must be added to lead-form partner list first; confirm legal entity (New Strata) |
