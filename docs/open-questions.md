# Open Questions

Boiled down from the 7/17 scoping call and post-call decisions, refreshed after the 7/22 follow-up. Two buckets: business/ops and technical (mostly access + specs Sean/Pier need to start unblocking now).
Last updated: 2026-07-28 (dialer-core decision recorded — A/B/C closed ✅, no ViciDial, T10 obsolete; see `PRD.md`).

> **7/22 session update:** the "session-2 agenda" items below were substantially covered. Cadence/dialing-pattern controls (Q8), IVA/contact-rate breakage (Q9), DID economics + recordings retention are now direction, not blank. Remaining hard-open: pilot vertical (Q1), "paused" definition (Q2), in/out of v1 (Q3), formal success thresholds (Q5), cost baselines (Q6), crediting rules (Q7), DNC inheritance (Q10).

## Business / ops questions

**Scope & pilot**
1. Which vertical is the pilot, specifically (bathroom vs windows — HW ruled out)? One campaign or several?
2. What does "dialing is paused" mean exactly — paused where, and what keeps running at KB/TD/CD during the build?
3. What is explicitly IN v1 vs deferred (QA/scoring, scheduling, compliance tooling)?
4. What starting volume slice do we take from the upstream split, and what performance gates scale it up?

**Success & economics**
5. What must be true at end of pilot to call v1 a success — which metrics, what thresholds?
6. Target cost/transfer (or cost/hour) vs current KB/TD/CD baselines — and what are those baselines? (Pull from the cost-analysis sheet / MDB.)
7. What counts as a billable/credited transfer with clients — the crediting rules the platform must record against.

**Operations**
8. Who operates it day-to-day, and what's the full control inventory (hours, lead flow, pacing, voice packs, scripts)? Which controls does Ashley need to be self-serve vs analyst-mediated?
9. Business definition of a "real" contact — the IVA/spam taxonomy that real-vs-fake classification must implement.
10. Compliance inheritance: which DNC/TCPA/state safeguards and client-specific constraints (e.g. Sunrun approved-URL rule) are hard requirements on day one?

**Test agenda (parking lot, needs prioritization)**
11. Branding: keep-cheap (AI-voice clips) vs generic vs drop — design the A/B test. **7/29 update:** dial-time pre-auth (decided at the Sean↔Pier sync) makes whole-call branding possible again — that was in fact the deciding argument. Now open: ❓ *how* branding is implemented (per-client branded clip sets in the voice pack? brand only in greeting vs throughout? which clients/verticals require it?), and ❓ validate Sean's hypothesis that branding lifts call response quality (Ashley's old test says yes — rerun as a platform A/B).
12. Bid/RPL-based client allocation vs round-robin — business appetite for revenue-weighted fairness.
13. Audit current fresh-routing round-robin behavior (Chicagoland 400-vs-3 skew) — assign (Brandon?).
14. Hot-transfer product (fresh leads, 45s script, new price) — when does it enter the roadmap?
15. Cadence: session-2 scheduling (early week of 7/20), demo checkpoints, and bandwidth guardrails vs Darwin/Meridius/analysis backlog.

## Technical questions

### Surfaces / APIs to get access to immediately

| # | Surface | Why now | Likely owner |
|---|---|---|---|
| T1 | **Pier's V1 AI build** — repo, prompts, Telnyx config, infra | It already dials and talks; it's the seed of option B and the fastest way to learn what Telnyx integration actually looks like. Also needs the ping-everything fix regardless. *LARGELY UNBLOCKED 2026-07-20: V1's Supabase (Retell-based, queue-orchestrated) deep-dived read-only — full write-up in `architecture/v1-build.md` (arch, June economics: ~$157/sale vs ~$25–35 at KB, dialing killed 7/8). Still needed from Pier: repo/prompts, Retell account+pricing, Telnyx config, agent naming context.* | Pier |
| T2 | **Telnyx account + API keys** (Call Control, SIP trunking, media streaming, voice-AI product, number ordering, pricing) | ~~Gates the A/B/C architecture decision~~ *(fork decided 7/23→7/27 — no ViciDial)* — keys now gate the soundboard/TTS cost model and PRD workstream W1. **CLOSED 2026-08-03** — Sean has portal access; `TELNYX_API_KEY` + `TELNYX_PUBLIC_KEY` live in `.env`, validated via `scripts/telnyx-check.ts` (key valid; billing-read failure turned out to be **no credit card on the account** — not a user-permission issue; **card landed 2026-08-07**). *Same day: outbound voice profile + Call Control app `3021367032303060958` provisioned (`scripts/telnyx-setup.ts`), test DID **+1 447 842 9621** purchased ($1 + $1/mo, `scripts/did-purchase.ts`) and active on the connection. Remaining for a live call: public webhook endpoint (IT) → roadmap step 2. Public-docs review: `architecture/telnyx-capability-review.md`. Still downstream: negotiated pricing, warm-transfer fee scope (billing surface).* | ~~Pier / IT~~ done |
| T3 | **LeadConduit account API access** (flows, entities, recipient config; Firehose eval) | We become a recipient endpoint — need the submission-doc/field-mapping spec and the lead payload contract (incl. OLeadID, sub-source fields). ***Payload contract received 2026-08-14** via Joseph's integration guide (`integrations/callcenter-integration-guide.md` §3.2): lead push carries oleadid, vendor_lead_code, FSCode1/2, contact/geo fields, and per-lead `max_attempts`; schema negotiable at onboarding. Remaining: LC account access itself, and ➤ confirm the push is LC recipient delivery (guide doesn't name LeadConduit).* | Joseph |
| T4 | **Pre-auth / client-selection endpoint spec** — request/response contract of the endpoint call centers hit today | *Timing decided 7/29 (Sean↔Pier↔Joseph): pre-auth fires at dial time on every dial — Joseph confirmed ~60M pings/mo is fine; re-check at qualification + fallback stays.* **CLOSED 2026-08-14** — full request/response contract received via Joseph's Confluence integration guide (`integrations/callcenter-integration-guide.md` §4.1): form-POST to `techsolarsolutions.com/API/transfer-client.php` with per-vertical `key` + zips/OLeadID/phone → ClientID/clientName/transferCode/transferPhone/vertical/State/Area/Zip/brandId/result; **fail closed** on timeout/error. Remaining at onboarding: our vertical key value; ❓ degraded-mode policy (fail-closed × dial-time-every-dial = outage halts dialing — cached pre-auth acceptable?). | ~~Joseph~~ done |
| T5 | **Command Center** — transfer priorities: where they live (API? techss_dl tables?), read/write surface | Platform must emulate/plug into it. | Joseph / Cromwel |
| T6 | **techss_ write-back contract** — target tables for dispositions/results (callcenter_dispos conventions, import-log pattern), push mechanics | Keeps MDB and every downstream dashboard alive; mirrors the TD_* ingestion pattern we already half-own. ***Transport half CLOSED 2026-08-14** via Joseph's integration guide (`integrations/callcenter-integration-guide.md` §4.2): post every call to `api.fivestrata.com/api/call-center/lead-disposition` (HTTP Basic, per-vertical creds; POST JSON; exactly once per call; required: calldispo_fives, oleadid, timestamp; full field dictionary + disposition-mapping rules captured; `repid_fives` explicitly admits an AI agent ID). ➤ No Answer is a first-class mapping scenario — no-contacts post (confirms the 8/13 audit open item, verify with Joseph). Remaining: the valid `calldispo_fives` dictionary (**Ashley owns it** — per the guide, contact her for current values) and the techss_-side landing (8/13 audit: Lead Intake logs to no table, EOD batch → MDB only).* | Joseph / Cromwel / Ashley |
| T7 | **Supabase org/project** access | Preferred app-layer home; need to size what it can host (hot store vs app data only). | Pier / Sean |
| T8 | **AWS + Snowflake** — account, warehouse, Snowpipe/S3 landing rights | Analytical tier for the 62M-rows/month fact stream; AutoWeb precedent lives here. *Owner identified (Pier, 7/23): **Shelly Teh** for Snowflake. Costs for Telnyx/Supabase/Snowflake must be outlined + approved via **Sam/Tatevik** — also the forcing function for the §5 cost baselines.* ***Snowflake half UNBLOCKED 2026-08-14** — Sean has enterprise-account access; scaffold built (approved by Sean 8/14): `scripts/snowflake-setup.sql` (paste-once Snowsight worksheet — role AICC_LOADER, warehouse AICC_WH X-Small/60s-suspend, db AICC.RAW + 4 mirror tables, key-pair service user AICC_SVC; all AICC_-prefixed) + `scripts/snowflake-check.ts` / `snowflake-sync.ts` (incremental watermark MERGE per `architecture/snowflake-value.md`; watermark derived from the TARGET, so moving the pipe to another account = .env change + re-run setup there). Remaining: Sean runs setup.sql, fills the 7 SNOWFLAKE_* vars in .env, first `npm run snowflake:sync`; AWS/S3 half (external stage/Snowpipe, Phase B) still open.* | Shelly Teh / Sam+Tatevik |
| T9 | **Ashley's daily dashboard** — the file + its data pulls | It IS the day-one reporting spec; also feeds Brandon/Alex's automation work. *File received 2026-07-20 (KB WI 3.1) — structure extracted to `reporting/kb-wi-dashboard-spec.md`, emulation views in `supabase/migrations/0002_reporting_views.sql`. Remaining: disposition dictionary (→T6), FS-code PD/CH/CP tag meanings, where the data pulls come from.* | Ashley |
| T10 | ~~**ViciDial source + a sandbox box** (AWS EC2; SCRATCH_INSTALL)~~ | **CLOSED — obsolete.** Purpose was to de-risk option A; the A/B/C fork closed with no ViciDial instance (7/23 Sean-Pier alignment, `ac4357e`; ✅ PRD Draft v1, Sean 2026-07-27, awaiting merge with Pier's draft). | — |
| T11 | **DNC/validation surface** — how LeadOps validates today (techss_all_leads dncDate et al.), and whether the platform gets pre-scrubbed leads only | Determines whether platform-side scrub is needed at all. ***Partial 2026-08-14:** Joseph's integration guide (§3.4–3.5) defines inbound DNC/unDNC push APIs we must host — suppression/restoration must take effect **immediately** (dial-queue pop-time check, not batch). Still open: whether inbound leads arrive pre-scrubbed (i.e. is the push the only DNC surface we need).* | Joseph |

### Design/spec questions (answerable once access lands)

- **Architecture fork (A/B/C)**: ✅ **DECIDED — no ViciDial instance** (hardened toward B in the Sean↔Pier chat 7/23 — "we'd have to work around the whole human-agent build" (Pier), `ac4357e`; **formalized in `PRD.md` Draft v1, Sean 2026-07-27**, awaiting merge with Pier's draft). VICI = schema vocabulary only (dispos, list/campaign language, `vendor_lead_code` ≡ OLeadID, compat views if needed); Supabase = orchestration/OLTP; Snowflake = analysis. T10 (VICI sandbox) closed.
- **AI conversation engine**: Telnyx-native vs own STT→LLM→TTS loop; latency budget per turn; how canned-clip playback interleaves with streaming TTS on the chosen media path. **Measured (8/7, first real clip calls — `scripts/clip-loop-test.ts`):** Call Control `playback_start` seams: **pre-queued ~550ms** (clip N+1 queued while N plays; format-independent — 22kHz and 8kHz WAV identical, so it's Telnyx queue-pickup floor, not decode), **reactive ~1450ms** via the dev loop (webhook→Postgres→250ms poll→~420ms command RTT from Sean's box), TTS `speak` seam 1300ms. Implication for the ≤~200ms gate: Call Control playback alone won't hit it — either **concatenate fixed clip sequences at authoring time** (no seam at all within an agent turn) or run audio ourselves via **media streaming** (the BYO half of the W1 bake-off, now sharply posed). Note the turn-taking seam (caller stops → agent clip starts) has a natural human budget of 200–500ms, which reactive-path co-location (step 3) plausibly reaches; the *intra-turn* clip seam is the one that must be ~0.
  **Round 2 (8/7 pm, conversational loop — `scripts/convo-loop-test.ts`):** (a) **MP3 clips cut the pre-queued seam to ~320ms** (vs ~550ms WAV — container, not sample rate, was the variable); (b) **Telnyx hosted AI works under our existing API key** — 25 LLMs (`/v2/ai/chat/completions`, Llama 3.3-70B etc.), TTS file generation (`/v2/text-to-speech/speech`, 4,450 voices incl. Polly Neural), so the "LLM key" dependency is CLOSED and W6 clip generation runs today (`scripts/gen-clips.ts`); (c) **the full soundboard-operator loop ran live**: caller speech → transcription → instant ack clip (latency mask) → LLM clip choice (1030ms, Llama-3.1-8B) → response — measured **turn seam 1597ms** via the dev path; (d) transcription **engine A (Google) returned empty transcripts** (confidence 0) — engine B (Telnyx) works; (e) **AMD accuracy 3/4**: human ✓, machine ✓✓ (voicemail), one false `machine` on a fast human answer — evaluate `premium` AMD; (f) **turn-taking design rule (Sean)**: never pre-queue behind a clip that invites a response — pre-queueing is for within-turn sequences only.
  **Round 3 (8/7 evening, calls 5–9 — live Sean-feedback iteration):** turn seam **1597ms → 465ms** across five calls. What did it: transcription starts at `call.answered` not after the greeting (catches immediate replies, kills engine spin-up from the perceived gap); **Deepgram engine** (engine B finals took 3–5s; Deepgram ~1.9s) **with `interim_results: true`** — the ack fires on the caller's *partial* transcript, before endpointing finalizes (biggest single win, seam 1300→465ms); ack on first sign of speech, LLM waits for the final. UX findings that generalize into the clip taxonomy: ultra-short clips ("Mm hm") arrive **garbled over telephony** — acks must be real 1–2s phrases; **connectors vs stalls** — "one second" promises a pause and is incongruous before immediate speech, so wait-promising clips are a separate category the engine fires only when the decision is actually slow; **seam tolerance is asymmetric** — callers are least tolerant of dead air right after answering a direct question; **variant rotation must avoid immediate repeats** (same ack twice in one call reads robotic — the PRD's 3–5-variant rule needs a no-repeat constraint). Remaining gap ≈ Deepgram partial latency (~1–2s) + dev-loop 465–790ms; co-location (step 3) targets the latter → near-human turn-taking without media streaming.
- **Voice pack pipeline**: batch TTS generation, clip storage/versioning, per-call canned-vs-TTS telemetry schema.
- **Warm-transfer leg**: bridging mechanics, client no-answer handling, transfer recording/crediting event schema (feeds business Q7).
- **Hot store choice**: ✅ **Supabase Postgres** (falls out of the A/B/C decision — see platform-foundations §6b). Still open: CDC route to Snowflake (nightly batch vs streaming; Snowpipe from S3 vs other) and hot-window length (30–90d).
- **Fact-stream schema**: the row-per-dial event model (ts, number, dispo, duration, session, script/prompt version, call center, client, geo, sub-source) + retention tiers (hot 30–90d → warehouse → archive).
- **DID management**: pool sizing for pilot volume, cap counters, rotation/retirement automation via Telnyx number APIs. **Open (7/22):** does the *carrier* (not Telnyx) impose a minimum hold — Ashley's "90-day" concern? Telnyx itself is a monthly subscription (swap anytime, pay the month). Design for individual benchmark-driven retirement, not block rotation. **✅ ANSWERED (7/31, td-windows replica, 90-day study — see `docs/reporting/td-windows-did-study.md`):** the *pool* never approaches 1,500/DID/day (typical daily max 25–40, median ~10 dials/active day across a ~10–11K standing pool), **but exactly one DID/day blows through it — the campaign default CID (18K dials/day peak, 173K/90d)**, which absorbs all unmatched-areacode traffic. So both 7/29 instincts were half-right. Design consequences: nightly Snowflake `retire_did` directive suffices (no intraday retirement machinery); pacer least-used-DID spreading is native; **fallback must be a pool, never a single default CID**. Bonus finds: monotonic answer-rate decay with dials/DID/day (30%→13% from ≤10 to ~200/day — campaign-confounded, stratified rerun pending), and SIP **603 Decline** (~1.5% of dials) as a free in-band carrier-reputation signal (= TD's `603lock`; answers Pier's number-reputation-API question internally). Remaining: ❓ optimal pool-size-vs-usage point (TD's ~10K pool ≈ $6–8K/mo at Telnyx bulk rates — beatable?); ❓ repeat on td-bathroom + KB. **New finding (8/7, first real call):** the platform's brand-new, never-dialed test DID displayed **"Likely Spam"** on Sean's handset on its very first call — baseline reputation for fresh VoIP DIDs is negative, not neutral. Design consequence: DID onboarding in the DID manager needs reputation registration as a first-class step (Free Caller Registry across the three carrier-analytics providers; Telnyx caller-ID/CNAM tooling; verify STIR/SHAKEN attestation level on our traffic), and per-DID benchmarks should track label status, not just answer rate. ❓ how much of the label clears with registration alone vs. warm-up volume/time — testable on the current DID before the pilot pool is bought. **Escalation observed same day:** after ~4 calls in ~40 minutes to the same handset, the carrier began diverting the DID **straight to voicemail** (answered in ~18s by VM, AMD `machine` in <1s) — reputation degradation is fast and compounding. CNAM listing enabled 8/7 (`FIVESTRATA`, `scripts/did-cnam.ts`; propagation takes days). Next lever: Free Caller Registry submission (web form — needs business contact info; Sean to submit).
- **Recordings**: storage target (S3?), stereo capture for QA/dead-air analysis. **Retention (7/22):** 5-year legal minimum; FiveStrata owns the backup (carrier-swap gap is the failure mode); hot 1–2-month window in a fast store, cold-archive the rest.
- **Tenant/program topology (7/23, see `architecture/tenant-program-onboarding.md`)**: playbook approval gate (who QA's a self-serve onboard before go-live); per-tenant secret management (env vars don't scale past tenant zero); per-tenant data isolation/retention/TCPA posture (auto vs home services); cost attribution per tenant-program; canonical disposition set — curated vs extensible; DID pools shared vs program-reserved (spam-reputation isolation between tenants).
- **Results DB shape (7/22):** confirmed a *separate* store (not techss_ results tables), keyed by OLeadID, on Snowflake, capturing every call; a DID dials/contact-rate view sits over the fact table; media partner tagged per call. Two-way write-back to techss_ still required for MDB/dashboards (see T6).
- **Contact-rate replacement**: IVAs make contact rate unreliable; evaluate connection rate as the primary signal and define the IVA/spam taxonomy (business Q9) so classification is well-specified.

### FS-code questions (added 2026-07-20; context in reporting/kb-wi-dashboard-spec.md)

*Updated 2026-07-20 from the fivestratadb profiled lookups (offline) — several answered without a live query.*

| # | Question | Status | Likely owner |
|---|---|---|---|
| F1 | `PD:` and `CH:` tag meanings | **Structure resolved (live query 2026-07-20), names still open.** PD is a channel GROUPING: PD:1 → {HS, IB, TM, RO}, PD:2 → {OS, SOC, SER, DIS, PUB, AFL, MIX, SP, SEM}, PD:5 → {CL}. Full CH list (15): AFL, CL, DIS, HS, IB, MIX, OS, PUB, RO, SEM, SER, SOC, SP, TM (+blank). Reads as PD:1 = phone-ish origins, PD:2 = web-media origins, PD:5 = client-provided. What PD and HS literally stand for → Brandon/Alex | Brandon / Alex |
| F2 | Tag contract / other tags | **Largely answered:** FSCode1 is consistently `\|VT\|PD\|CH\|SC\|CP\|` in that order (rare truncation: missing CP). Additional tags live in FSCode2/3, not FSCode1: `SS` (numeric sub-source id), `SA` (sub-affiliate id), `C0`/`CC` (call center: KB/TD/CD/YD), `BT` (batch tag). Confirm the writer enforces order | Joseph |
| F3 | Who mints it, where; generator spec/dictionary | Open — but `techss_all_leads.unique_FSCode1` / `unique_FSCodes` views already decompose codes into key/value pairs (a live dictionary to read) | Joseph |
| F4 | FSCode1 vs 2 vs 3 roles | **Answered structurally:** FSCode1 = acquisition identity (media taxonomy). FSCode2 = source detail + call-center assignment (`\|SS:…\|SA:…\|C0:KB\|`). FSCode3 = distribution batch provenance (`\|BT:RU_RV_BR_CC39_03292026\|CC:TD\|` — revive batch name + call center). Confirm semantics with Joseph | Joseph / Cromwel |
| F5 | SC canonical dictionary | **Partial:** `unique_FSCode1`/`unique_FSCodes` are maintained TABLES (not views): FSCode1 has a per-key dictionary (VT 9, PD 4, CH 15, SC 122, CP 270); `unique_FSCodes` stores co-occurrence PAIRS (SC→CP, PD→CH, VT→call-center) — useful, but NOT a code→partner-name map. Partner names still = Command Center affiliates lookup — readable table/API? | Alex / Joseph |
| F6 | Immutability | **Strong inference:** routing/batch assignment is stamped into FSCode2 (C0) and FSCode3 (BT/CC), leaving FSCode1 as immutable acquisition identity. Confirm nothing rewrites FSCode1 | Joseph |
| F7 | `client_blocked_fscodes` enforcement point | **Structure + purpose answered.** The table serves exactly ONE client — **Sunrun (clientId 9)**, 317 SC×CP rules dating to 2017. Context (Sean, 2026-07-20): when Sunrun had to turn us off for a while, the easy path was to keep receiving their setup and **soft-disable via FS-code rules** so it could be flipped back on — i.e. the mechanism was actively enforced, not dormant. The `approved` flag is the on/off per SC×CP. Also found: `techss_dl.client_approved_urls` (505 URL rules, Momentum Solar #425 + SunPro #475) — the "URL approval" feature Kinsey called hard-to-build has tables. Remaining: WHERE in the flow enforcement happens (LeadOps? distribution query?) so the platform mirrors it, and whether client_approved_urls is likewise enforced | Joseph |
| F8 | LeadConduit payload fields for FS codes | Open | Joseph |
| F9 | Cardinality | **Answered (live 2026-07-20):** dictionary sizes VT 9, PD 4, CH 15, SC 122, CP 270; current revive inventory (`RVInvtyRevive`) spans only 106 distinct FSCode1 across 2,120 rows. Trivial dimensions | — |

**Bonus find (2026-07-20 live queries): `techss_dl.clients` is the client-dimension blueprint** for the platform's client model — it already carries `branding` (the spoken brand name), `silentTransfer`, `transferCode/transferPhone/transferDays/transferStartHour/transferEndHour` (transfer windows), `clientThrottle`, `acceptRate`, `auditRate`, and `LeadConduitCode`. Our `clients`/`transfer_priorities` schema should align with these semantics (feeds T4/T5 and the two-phase selection design).
